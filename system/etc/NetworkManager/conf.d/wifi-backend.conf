# WiFi backend = iwd. wpa_supplicant ile boot'ta DHCP ~14sn gecikiyordu
# (link associate olduktan sonra ilk DHCP DISCOVER kaybolup retry bekliyordu).
# iwd ile link gercekten hazir olunca ilk DHCP aninda basariyor: 14sn -> 0.2sn.
[device]
wifi.backend=iwd
