# WiFi backend = iwd. With wpa_supplicant, DHCP was delayed ~14s on boot (after
# link association the first DHCP DISCOVER was lost and it waited for a retry).
# With iwd the link is truly ready when DHCP starts, so the first request
# succeeds immediately: 14s -> 0.2s.
[device]
wifi.backend=iwd
