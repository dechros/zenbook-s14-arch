# CS:GO hard-freeze diagnosis: panic if a detectable lockup occurs (a panic
# leaves a trace on screen/in the log). softlockup_panic is deliberately OMITTED:
# under heavy game load it can false-positive and panic unnecessarily.
kernel.panic_on_oops = 1
kernel.hardlockup_panic = 1
