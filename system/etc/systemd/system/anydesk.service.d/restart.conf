# anydesk crashed periodically with SIGABRT and the stock unit has no Restart ->
# systemd did not bring it back (a manual restart was needed). Endless self-heal:
# restart 5s after every crash/exit and never hit the start limit (never give up).
[Unit]
StartLimitIntervalSec=0

[Service]
Restart=always
RestartSec=5
