# Lunar Lake Arc 140V: ollama drops integrated GPUs by default ("dropping
# integrated GPU"). This machine has no discrete GPU, so the iGPU with LPDDR5X
# is the only option.
[Service]
Environment="OLLAMA_IGPU_ENABLE=1"
