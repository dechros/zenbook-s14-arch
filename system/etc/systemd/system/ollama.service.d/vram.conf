# One model resident at a time, held for 30 minutes. Loading a second model
# pushes the first into CPU memory and it never migrates back, so the box keeps
# serving from the CPU until ollama restarts.
[Service]
Environment="OLLAMA_MAX_LOADED_MODELS=1"
Environment="OLLAMA_KEEP_ALIVE=30m"
